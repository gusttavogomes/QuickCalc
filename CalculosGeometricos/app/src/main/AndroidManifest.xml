<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aluno.calculosgeometricos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuActivity" />
        <activity
            android:name=".HistoricoActivity"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".ListFiguras"
            android:parentActivityName=".MenuActivity" />
        <activity
            android:name=".ListFigura"
            android:parentActivityName=".ListFiguras" />
        <activity android:name=".UmCampoActivity" />
        <activity android:name=".ResultadoActivity" />
        <activity android:name=".DoisCamposActivity" />
        <activity android:name=".TresCamposActivity" />
        <activity android:name=".TipoTrianguloActivity"></activity>
    </application>

</manifest>